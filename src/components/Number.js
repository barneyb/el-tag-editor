import React from "react";

const Tag = ({
                 n,
             }) =>
    <span style={{
        marginLeft: "3px",
        paddingLeft: "3px",
        borderLeft: "1px solid #09f",
    }}>{n}</span>;

export default Tag;
